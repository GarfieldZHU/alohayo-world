# Contributing

1. Read `AGENTS.md` and the architecture decisions.
2. Keep engine behavior deterministic and configuration-driven.
3. Run `yarn format:check && yarn lint && yarn typecheck && yarn validate:content && yarn test`.
4. Register every asset in `assets/ATTRIBUTION.json`.
5. Add an ADR before changing a foundational architectural decision.
